# pux

Package CI failures into AI-ready local context.

## Start

```sh
npm install
npm run dev -- watch
```

Useful starter commands:

```sh
npm run dev -- doctor
npm run typecheck
```

This first scaffold provides the TypeScript CLI entry point, Ink-powered live terminal interface, and environment checks. GitHub Actions watching and context-file generation come next.
